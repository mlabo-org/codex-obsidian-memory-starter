# Demo

このディレクトリは任意の短い動画 demo の置き場です。

最小構成では demo は必須ではありません。まずは `assets/screenshots/` の 2 枚で十分です。

もし追加するなら、次のような 1 本だけで足ります。

- `prompt-to-episode.mp4`

内容:

1. Codex へ prompt を送る
2. `ultra_short` に反映される
3. 会話の区切り後に `episodes` に追記される

長さは 30 秒未満を推奨します。
